generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
}

model Transaction {
  id           String   @id @default(uuid())
  total_amount Float
  status       String
  created_at   DateTime @default(now())
}